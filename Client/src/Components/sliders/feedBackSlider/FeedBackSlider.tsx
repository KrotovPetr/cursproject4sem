import React, { FC } from 'react';
import styles from '../slider/Slider.module.scss';

const FeedBackSlider: FC = () => {
    return <div className={styles.slider}>1</div>;
};

export default FeedBackSlider;
