import React, {FC} from 'react';
import styles from './menu.module.scss';
const Menu:FC = () => {
    return (
        <div className={styles.menu}>
            
        </div>
    );
};

export default Menu;